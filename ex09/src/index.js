const users = {id: 1, firstName: "John", lastName: "Doe", age: 24, gender: "male"};
const id = users.id;
const firstName = users.firstName;
const lastName = users.lastName;
const age = users.age;
const gender = users.gender;

// Lodash
const _ = require('lodash');

// Only change code below this line
// users nested array with four objects starts here

// users nested array with four objects ends here

// getUser function - list of users starts here 


// getUsers function - list of users ende here

// findUser(id) function starts here
function findUserById(ide) {
    try {
        // add appropriate code here 

    } catch (error) {
        return // Change this line
        console.log(); // Change this line
    }
}
// findUsersByID(id) function ends here
// Only change code above this line
getUsers();
findUserById(); // Change this line

module.exports = findUserById;